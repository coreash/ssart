import React from 'react';
import AppRouter from './router';

const Header = () => {
    return (
        <header>
            Header area
            <AppRouter />
        </header>
    );
}

export default Header;